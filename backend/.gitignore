# Virtual Environments
.venv/

# Python cache
__pycache__/
*.pyc

# Training data (too large for git)
data/

# Model files — ignore local copies (frontend/public/models/ is used for deployment)
models/*.pth
models/*.onnx
models/*.onnx.data
models/*:Zone.Identifier

# Jupyter
.ipynb_checkpoints/
